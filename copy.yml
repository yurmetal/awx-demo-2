---
    - name: Copy file from source to destination
      hosts: awx-client.localdomain
      remote_user: jdoe
      become: true

      tasks:
        - name: Start copy
          raw: ping -c 5 8.8.8.8